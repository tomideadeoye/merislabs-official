---
description: Finalize and Export NICArb Box Assets
---

1. Verify orientation in `constants.ts` (Should be 400x300).
2. Run `box-calculator.ts` to ensure all panel scales are correct.
3. Check `TopPanel.tsx` for "Variant A" design elements.
4. Verify 3D lid hinge in `Box3DPreview.tsx`.
5. Generate high-res production exports.
